#Before starting install docker-machine and run docker on your computer:
#brew install docker-machine


# 1. Create a virtual machine with docker-machine using the virtualbox driver, and named Char.

#docker-machine create docker && \
#docker-machine start docker
docker-machine create --driver=virtualbox Char

# We created a container named Char by using doker-machine and we attached it to virtual boxsh 01
# to check "docker-machine ls "